import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 62,
    marginBottom: -62,
    justifyContent: 'flex-end',
    alignItems: 'center',
  },

  canvas: {
    width: 64,
    height: 335,
    position: 'absolute',
    zIndex: 1
  }
})